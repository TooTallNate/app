type User {
  license: String!
  name: String!
}

input LoginInput {
  username: String!
  password: String!
}

type Query {
  user: User!
}

type Mutation {
  login(input: LoginInput!): User!
  logout: Boolean!
}